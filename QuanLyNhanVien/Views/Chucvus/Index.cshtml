@model IEnumerable<QuanLyNhanVien.Models.Chucvu>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tenchucvu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenchucvu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idcv">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idcv">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idcv">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
